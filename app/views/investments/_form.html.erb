  <%= simple_form_for(investment, method: :post, url: project_investments_path)  do |f| %>
      <%= f.input :number_of_panels %>
      <%= f.button :submit, "Buy now", class: "btn btn-primary" %>
  <% end %>
