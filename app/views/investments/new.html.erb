<div class="container">
  <%= link_to 'Back', :back %>
  <br>
  <p>Please fill in your investment selection for project
  <%= @project.name %></p>
  <p>
    Project size:
    <%= @project.panels_quantity %> panels
  </p>
  <p>Availability:
    <%= @project.available_panels%> panels
  </p>
  <p>Price per panel: <%= humanized_money_with_symbol(@project.price) %></p>
  Quantity to invest:
  <%= render "investments/form", investment: @investment %>
</div>
